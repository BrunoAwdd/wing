# URL do backend Wing (Deno/Oak). Em dev local, precisa estar na allowlist
# de DEVELOPMENT_ORIGINS de backend/src/config/corsConfig.ts (já inclui
# https://localhost:5174, a porta padrão do dev server deste site).
VITE_API_BASE_URL=http://localhost:3005
